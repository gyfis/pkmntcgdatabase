<nav class="navbar navbar-light secondary-panel">
    <div class="container">
        <div class="row">
            <div class="col-xs-offset-2 col-xs-"></div>
        </div>
        <ul class="nav navbar-nav">
            <li class="nav-item"><a class="nav-link" href="{{ url_for('index') }}">Home</a></li>
            <li class="nav-item"><a class="nav-link" href="{{ url_for('list_cards') }}">Cards</a></li>
            {% if current_user.is_authenticated %}
                <li class="nav-item pull-right"><a class="nav-link" href="{{ url_for('logout') }}">Log out</a></li>
                <li class="nav-item pull-right"><a class="nav-link" href="{{ url_for('profile') }}">Profile</a></li>
                <li class="nav-item pull-right"><a class="nav-link" href="{{ url_for('user_cards', path=current_user.username) }}">Your cards</a></li>
{#                <li class="nav-item pull-right"><a class="nav-link" id="copy-address" style="cursor: pointer">Copy user address to clipboard ( /u/{{ current_user.username }} )</a></li>#}
            {% else %}
                <li class="nav-item pull-right"><a class="nav-link" href="{{ url_for('signup') }}">Sign Up</a></li>
                <li class="nav-item pull-right"><a class="nav-link" href="{{ url_for('login') }}">Login</a></li>
            {% endif %}
        </ul>
    </div>
    <div class="container text-right">

    </div>
</nav>